name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/docker-login@v1
        with:
          login-server: "knowledgefactoryregistry.azurecr.io"
          username: "KnowledgefactoryRegistry"
          password: "ryrIFK+udeRsLrqqNVKqifn8T60DuR2Si3tAfTEUFT+ACRD7eSyW"
      - run: |
          docker build . -t knowledgefactoryregistry.azurecr.io/knowledgefactory:${{ github.sha }}
          docker push knowledgefactoryregistry.azurecr.io/knowledgefactory:${{ github.sha }}